# Real execution is enabled by default for integration testing and production-like runs.
# Operators can still disable either engine explicitly with environment variables.
platform.seatunnel.real-enabled=${SEATUNNEL_REAL_ENABLED:true}
platform.scheduler.dolphinscheduler.real-enabled=${DOLPHINSCHEDULER_REAL_ENABLED:true}
